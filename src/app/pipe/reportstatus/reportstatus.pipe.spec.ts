import { ReportstatusPipe } from './reportstatus.pipe';

describe('ReportstatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ReportstatusPipe();
    expect(pipe).toBeTruthy();
  });
});

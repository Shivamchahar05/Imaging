import { ReportdetailPipe } from './reportdetail.pipe';

describe('ReportdetailPipe', () => {
  it('create an instance', () => {
    const pipe = new ReportdetailPipe();
    expect(pipe).toBeTruthy();
  });
});
